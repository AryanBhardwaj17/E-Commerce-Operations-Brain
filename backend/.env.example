# Backend-local environment example.
# For full-stack runs from the repo root, prefer copying `../.env.example` to `../.env`.
# Keep this file backend-scoped if you want to run the backend service in isolation.

# ─────────────────────────────────────────────
#  E-Commerce Operations Brain — Backend Environment
# ─────────────────────────────────────────────

# ── LLM Provider (choose one: "azure", "openai", or "ollama") ────────────────
LLM_PROVIDER=azure

# ── Azure OpenAI ─────────────────────────────────────────────────────────────
AZURE_OPENAI_ENDPOINT=https://your-azure-openai-endpoint.openai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
OPENAI_API_VERSION=2025-04-01-preview
AZURE_OPENAI_DEPLOYMENT=your-chat-deployment-name
AZURE_EMBEDDING_DEPLOYMENT=your-embedding-deployment-name

# ── Standard OpenAI (used only when LLM_PROVIDER=openai) ─────────────────────
OPENAI_API_KEY=your-openai-api-key

# ── Ollama (used only when LLM_PROVIDER=ollama) ───────────────────────────────
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=llama3.2
LLM_TEMPERATURE=1.2
LLM_MAX_TOKENS=2000

# LangSmith (observability)
LANGSMITH_API_KEY=your-langsmith-api-key
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGSMITH_PROJECT=ecommerce-ops-brain
LANGSMITH_TRACING=true

# PostgreSQL — LangGraph checkpointer
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ops_brain
POSTGRES_USER=ops_brain
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql://ops_brain:change-me@localhost:5432/ops_brain
CHECKPOINT_BACKEND=auto
REPOSITORY_BACKEND=postgres
REPOSITORY_SCHEMA=commerce

# ChromaDB
CHROMADB_HOST=localhost
CHROMADB_PORT=8001

# LangGraph state encryption (optional — leave blank to disable)
LANGGRAPH_AES_KEY=

# API
API_KEY=change-me
CORS_ALLOWED_ORIGINS=http://localhost:3000
RUNTIME_STORE_DIR=.runtime
APP_ENV=development
LOG_LEVEL=INFO

# OpenTelemetry
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTEL_SERVICE_NAME=ecommerce-ops-brain
